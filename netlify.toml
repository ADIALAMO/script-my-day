# netlify.toml

# Build configuration for the static site
[build]
  publish = "public"
  command = "echo 'Netlify is building your static site...'"

# Functions configuration for Netlify Functions
[functions]
  # Directory where your serverless functions are located
  directory = "netlify/functions"

  # The following properties (timeout, external_node_modules, node_bundler)
  # are temporarily removed due to persistent parsing errors in Netlify.
  # If the build succeeds with this minimal configuration, we can try to
  # add them back one by one if specific issues (like timeouts or module not found)
  # resurface later.
