{
  "name": "my-life-script",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/generatescript.cjs",
    "dev": "vercel dev",
    "build": "echo 'No build required' && exit 0",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}