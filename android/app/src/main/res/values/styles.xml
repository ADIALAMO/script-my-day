<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Theme.SplashScreen (from androidx.core:core-splashscreen, which
         @capacitor/splash-screen pulls in) reads the Android 12+ platform
         SplashScreen API from windowSplashScreenBackground/
         windowSplashScreenAnimatedIcon — NOT android:background, which is
         the legacy pre-12 attribute. Setting only android:background left
         those two unresolved, so Android 12+ silently fell back to its own
         auto-generated default (a light background + auto-derived icon) —
         confirmed on-device before this fix. windowSplashScreenAnimatedIcon
         takes an icon-only drawable, not the full composited splash image
         (the OS composites icon-on-background itself) — ic_launcher_foreground
         is exactly that, already generated by @capacitor/assets in Phase 1.
         android:background is kept for pre-12 devices, where it's still the
         real mechanism. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/brand_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    </style>
</resources>